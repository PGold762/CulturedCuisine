

  <!-- Regional (cuisines) pages -->


  <!-- Regional (cuisines) pages -->
  <div id="otherCuisines" class="col-12 col-md-12 col-lg-9">
    <div class="oc-heading">
      <h2 class="explore-heading">Explore More Cuisines</h2>
    </div>
    <div class="cuisine-container" id="cuisine-container">

      <!-- Cards will be dynamically generated here -->


      {{#each recipes as |Recipe|}}
      <div class="cuisine">
        <h3>{{title}}</h3>
        <a href="/recipe/{{Recipe.id}}">
          {{!-- <img src="{{recipe.imageUrl}}" alt="{{recipe.name}}"> --}}
          <div class="cuisine-info">
            <img src="{{Recipe.image}}" alt="{{Recipe.title}}">
          </div>
        </a>

      </div>
      {{/each}}


      {{!-- function nextQuestion() {
      mainText.textContent = questions[index].question;
      // answerButtons.textContent= questions[index].choices[0];
      for (let i = 0; i < 4; i++) { var button=document.createElement("button");
        button.textContent=questions[index].choices[i]; answerButtons.append(button); button.addEventListener("click",
        ()=> {
        index++;
        answerButtons.innerHTML = "";

        nextQuestion();
        });
        } --}}

        </main>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
        <script src="/js/script.js"></script>
        <script src="https://unpkg.com/@splidejs/splide@2.4.12/dist/js/splide.min.js"></script>
        <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>

        {{#if signed_in}}
        <script src="/js/signout.js"></script>
        {{/if}}
    </div>
  </div>

